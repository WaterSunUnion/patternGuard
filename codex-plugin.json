{
  "name": "patternguard",
  "version": "1.0.0",
  "description": "Guard AI-agent code changes against pattern drift: plan the blast radius, execute within bounds, and review sibling consistency before committing.",
  "author": {
    "name": "Calvin",
    "email": "yiwen.calvin@gmail.com",
    "url": ""
  },
  "license": "MIT",
  "keywords": [
    "scope",
    "pattern",
    "template",
    "impact-analysis",
    "blast-radius",
    "agentic",
    "codebase",
    "planner",
    "executor",
    "reviewer",
    "index",
    "subagent"
  ],
  "skills": "./skills/",
  "interface": {
    "displayName": "PatternGuard",
    "shortDescription": "Guard agent code changes against pattern drift",
    "longDescription": "Three coordinated skills for codebases operated by AI agents and subagents.\n\npatternguard-planner — Maps every file a change touches before writing a single line. Classifies logic (route-specific, cross-cutting, utility), chooses between template generation (R1), index contract (R2), and earned abstraction (R3). Outputs CODEBASE_INDEX.yaml, IMPACT_MAP.yaml, and a structured handoff note. Works on greenfield and existing projects.\n\npatternguard-executor — Executes code changes with full scope awareness. Self-selects between four modes: Bootstrap (existing project, no index), Solo (simple task, no subagents), Orchestrator (large scope, spawns subagents with task envelopes), Subagent (received an envelope, executes one file). Only the orchestrator writes to index files.\n\npatternguard-reviewer — Reviews changes after execution. Verifies correctness, completeness, and sibling consistency. Never edits code — only reads and produces verdicts: approved, changes-needed, or escalate. Self-selects between Solo, Orchestrator, and Subagent modes.",
    "developerName": "Calvin",
    "category": "Productivity",
    "capabilities": [
      "Read",
      "Write"
    ],
    "defaultPrompt": [
      "Use patternguard-planner to map the structure for a new payment validation feature",
      "Use patternguard-executor to apply the fraud threshold change across all routes",
      "Use patternguard-reviewer to verify the executor's changes before committing"
    ],
    "brandColor": "#FF6B35"
  }
}
